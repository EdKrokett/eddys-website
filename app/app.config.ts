export default defineAppConfig({
  ui: {
    // Platzhalter — wird ersetzt, sobald die Design-Richtung feststeht (siehe CLAUDE.md,
    // Abschnitt "Design-Priorität").
    colors: {
      primary: 'neutral',
      neutral: 'neutral',
    },
  },
})
